#!/usr/bin/python3
# run a while loop that is always true
while(True):
    # take input for number
    number = int(input())
    
    # terminate the loop if the user input is 0
    if number == 0:
        break
    # print the number
    print(number)
